{% assign max_price = -1 %}{% for variant in dm_orig_product.variants %}{% capture variant_price %}{% render 'dla_dm_variant_price', dm_rounding: true, dm_orig_product: dm_orig_product, dm_orig_variant: variant %}{% endcapture %}{% assign variant_price = variant_price | abs %}{% if variant_price > max_price %}{% assign max_price = variant_price %}{% endif %}{% endfor %}{{ max_price | round }}